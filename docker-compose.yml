services:
  web:
    container_name: web
    build:
      context: .
    ports:
      - "8000:8000"
    command: ["sh", "-c", "./wait-for-it.sh cityops_db:5432 -- ./start.sh"]
    volumes:
        - .:/app
    env_file:
      - .env
    depends_on:
      - cityops_db

  cityops_db:
    container_name: database
    image: postgis/postgis
    platform: linux/amd64
    restart: always
    volumes:
      - cityops_db_data:/var/lib/postgresql/data
    environment:
        POSTGRES_USER: ${POSTGRES_USER}
        POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
        POSTGRES_DB: ${POSTGRES_DB}

volumes:
  cityops_db_data:
